# Troubleshooting

Start with `configuration_validator`, `diagnostics`, database migration status and API capability checks. Do not enable live orders while health checks, HMAC verification or idempotency storage are failing.
