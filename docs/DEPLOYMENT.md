# Deployment

Run behind TLS, expose only the webhook listener, verify Shopify HMAC, encrypt AliExpress and Shopify tokens, configure backups, then enable live orders only after end-to-end sandbox validation.
